package uparis.diamanthamdi.frontend.observer;

/**
 * Observer for the scale.
 * 
 * This observer is used to notify the visualizer to display the scale in the visualizer.
 */
public interface ScaleObserver {
    void update(int scale);
}
